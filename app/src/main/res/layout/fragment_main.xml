<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main_modern"
    android:padding="24dp">

    <!-- ========================= -->
    <!--        HEADER AREA        -->
    <!-- ========================= -->
    <LinearLayout
        android:id="@+id/headerContainer"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- LOGO -->
        <ImageView
            android:id="@+id/logo"
            android:layout_width="130dp"
            android:layout_height="130dp"
            android:src="@drawable/ic_logo"
            android:contentDescription="@string/app_name"
            android:padding="4dp"
            android:layout_marginBottom="8dp" />

        <!-- MAIN TITLE -->
        <TextView
            android:id="@+id/titleText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FLARE"
            android:textSize="42sp"
            android:textStyle="bold"
            android:letterSpacing="0.08"
            android:textColor="@android:color/white"
            android:shadowColor="#66000000"
            android:shadowDx="0"
            android:shadowDy="3"
            android:shadowRadius="6" />

        <!-- SUB TITLE / LOCATION -->
        <TextView
            android:id="@+id/subTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="TAGUM • 2025"
            android:textSize="14sp"
            android:textColor="@android:color/white"
            android:alpha="0.85"
            android:layout_marginTop="2dp" />

        <!-- TAGLINE -->
        <TextView
            android:id="@+id/tagline"
            android:layout_width="260dp"
            android:layout_height="wrap_content"
            android:text="Rapid fire response at your fingertips."
            android:textSize="15sp"
            android:textColor="@android:color/white"
            android:alpha="0.85"
            android:gravity="center"
            android:lineSpacingExtra="2dp"
            android:letterSpacing="0.02"
            android:layout_marginTop="6dp" />

    </LinearLayout>


    <!-- ========================= -->
    <!--     GLASS AUTH CARD       -->
    <!-- ========================= -->
    <LinearLayout
        android:id="@+id/cardContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="@drawable/bg_glass_card"
        android:elevation="12dp"
        android:clipToOutline="true"
        app:layout_constraintTop_toBottomOf="@id/headerContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp">

        <!-- CARD TITLE -->
        <TextView
            android:id="@+id/cardTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Get started"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:letterSpacing="0.03"
            android:layout_marginBottom="4dp" />

        <!-- CARD SUBTITLE -->
        <TextView
            android:id="@+id/cardSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Log in to report incidents or register as a new user."
            android:textSize="13sp"
            android:textColor="@android:color/white"
            android:alpha="0.85"
            android:layout_marginBottom="18dp" />

        <!-- LOGIN BUTTON -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/loginButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="@string/login"
            android:textSize="16sp"
            android:textAllCaps="false"
            style="@style/Widget.MaterialComponents.Button"
            android:backgroundTint="@color/primaryColor"
            android:textColor="@android:color/white"
            app:cornerRadius="20dp"
            android:layout_marginBottom="12dp" />

        <!-- REGISTER BUTTON -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/registerButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="@string/register"
            android:textSize="16sp"
            android:textAllCaps="false"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            app:cornerRadius="20dp"
            android:backgroundTint="@android:color/transparent"
            app:strokeColor="@android:color/white"
            app:strokeWidth="2dp"
            android:textColor="@android:color/white" />

    </LinearLayout>


    <!-- ========================= -->
    <!--  BOTTOM QUICK ACTIONS     -->
    <!-- ========================= -->
    <LinearLayout
        android:id="@+id/bottomActions"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/cardContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp">

        <!-- SMS REPORT LINK -->
        <TextView
            android:id="@+id/smsReport"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Send SMS Fire Report"
            android:textSize="15sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:padding="8dp"
            android:background="?attr/selectableItemBackgroundBorderless" />

        <!-- STATION CONTACT LINK -->
        <TextView
            android:id="@+id/stationContact"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Find nearest fire station"
            android:textSize="15sp"
            android:textColor="@android:color/white"
            android:alpha="0.9"
            android:padding="8dp"
            android:layout_marginTop="4dp"
            android:background="?attr/selectableItemBackgroundBorderless" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
